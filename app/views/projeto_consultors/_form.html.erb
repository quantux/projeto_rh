<%= form_with(model: projeto_consultor, local: true) do |form| %>
  <% if projeto_consultor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(projeto_consultor.errors.count, "error") %> prohibited this projeto_consultor from being saved:</h2>

      <ul>
        <% projeto_consultor.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :projeto_id %>
    <%= form.number_field :projeto_id %>
  </div>

  <div class="field">
    <%= form.label :consultor_id %>
    <%= form.number_field :consultor_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
